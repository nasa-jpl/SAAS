[project]
name = "syssim"
description = "General purpose multi-system simulation utilizing graph structure."
version = "2.0.0"
authors = [
    { name = "Josef Biberstein", email = "josef.business@protonmail.com" }
]
dependencies = [
    "matplotlib",
    "rustworkx",
    "toml",
    "rich",
    "numpy"
]

[project.optional-dependencies]
test = [
    "pytest>=7.0",
    "scipy"
]